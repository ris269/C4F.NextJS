import React from "react";
import { text } from "stream/consumers";

type Props = {};

export default function AppFooter({}: Props) {
  return (
    <div
      style={{ backgroundColor: "#ddd", textAlign: "center", padding: "5px 0" }}
    >
      NextJS Series
    </div>
  );
}
